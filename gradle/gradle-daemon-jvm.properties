# Gradle Daemon JVM settings
# This file controls the JVM used for the Gradle daemon
# We need to trick Gradle into thinking we're using Java 21

# Set the JVM arguments for the daemon
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Disable the daemon to avoid version detection issues
org.gradle.daemon=false